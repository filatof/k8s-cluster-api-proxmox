apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cluster-api-prod

resources:
- ../../base

namePrefix: prod-

patchesStrategicMerge:
- cluster-patch.yaml
- control-plane-patch.yaml
- workers-patch.yaml

commonLabels:
  environment: prod